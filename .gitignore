# Fichiers et dossiers à ignorer pour ce projet Vite + React

# — Dépendances Node —
node_modules/

# — Artefacts de build —
dist/
dist-ssr/
build/

# — Caches d’outils —
node_modules/.vite/
.vite/
.vitest/
.cache/
.parcel-cache/
.turbo/
.eslintcache
.stylelintcache
*.tsbuildinfo

# — Tests et couverture —
coverage/

# — Logs —
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*
report.*.json

# — Fichiers d’environnement —
.env
.env.*
!.env.example
!.env.sample
# Conserver seulement des exemples, pas les secrets locaux
.env.local
.env.*.local

# — Éditeurs/IDE —
# JetBrains (WebStorm, IntelliJ)
.idea/
*.iml
# VS Code
.vscode/
.history/

# — OS spécifiques —
.DS_Store
Thumbs.db

# — Hébergement local —
.vercel/
.netlify/

# — Divers —
# Ignorer les sorties temporaires variées
*.tmp
*.temp
~$*

# — Archives et fichiers temporaires supplémentaires —
*.tgz
*.swp
*.swo

# Dossiers temporaires
.tmp/
.temp/
tmp/
temp/

# — OS Windows supplémentaires —
desktop.ini
ehthumbs.db
